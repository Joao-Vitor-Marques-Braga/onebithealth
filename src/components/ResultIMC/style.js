import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    textInformations:{
        fontSize: 15,
        textAlign: 'center'
    },
    resultIMC:{
        textAlign: 'center',
        color: "#D90D4A",
        fontSize: 50,
        fontWeight: 'bold'
    }
})

export default styles